<Window x:Class="LeitorArquivos.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Leitor de Arquivos" Height="450" Width="800"
        Background="#F3F4F6"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="filePath"
                     Grid.Column="0"
                     Height="38"
                     Padding="10,0"
                     VerticalContentAlignment="Center"
                     FontSize="14"
                     BorderBrush="#CBD5E1"
                     BorderThickness="1"
                     Margin="0,0,10,0"/>

            <Button x:Name="file"
                    Grid.Column="1"
                    Click="save_Click"
                    Content="Salvar"
                    Width="100"
                    Height="38"
                    Margin="0,0,10,0"
                    Background="#3B82F6"
                    Foreground="White"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"/>

            <Button x:Name="load"
                    Click="load_Click"
                    Grid.Column="2"
                    Content="Carregar"
                    Width="100"
                    Height="38"
                    Background="#10B981"
                    Foreground="White"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </Grid>

        <TextBox x:Name="contend"
                 Grid.Row="1"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Padding="10"
                 FontSize="14"
                 FontFamily="Consolas"
                 BorderBrush="#CBD5E1"
                 BorderThickness="1"/>
    </Grid>
</Window>